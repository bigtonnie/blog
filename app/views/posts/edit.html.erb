<h1 class='mt-4 mb-2 py-2'>Edit Blog Post</h1>
<%= render partial: 'form', locals: {post: @post} %>
<%= button_to 'Delete', @post, class: 'py-2 px-4 bg-red-500 text-white rounded-md my-1', method: :delete, data: {turbo_confirm: 'Are you sure?'} %>
